name: Android Kernel Module CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-22.04
    defaults:
      run:
        shell: bash
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
        continue-on-error: false

      # 核心修复：删除md5sum（系统内置）+ 优化换源命令确保生效
      - name: Install base dependencies
        run: |
          # 优化换源命令（适配所有源地址格式，确保替换成功，避免azure源卡顿）
          sudo sed -i 's@http://archive.ubuntu.com/ubuntu/@http://mirrors.aliyun.com/ubuntu/@g' /etc/apt/sources.list
          sudo sed -i 's@http://security.ubuntu.com/ubuntu/@http://mirrors.aliyun.com/ubuntu/@g' /etc/apt/sources.list
          sudo sed -i 's@https://archive.ubuntu.com/ubuntu/@http://mirrors.aliyun.com/ubuntu/@g' /etc/apt/sources.list
          sudo sed -i 's@https://security.ubuntu.com/ubuntu/@http://mirrors.aliyun.com/ubuntu/@g' /etc/apt/sources.list
          
          # 强制更新源缓存，修复索引缺失
          sudo apt-get update -y --fix-missing --allow-releaseinfo-change
          #  删除md5sum（系统内置，无需安装），保留核心依赖
          sudo apt-get install -y --no-install-recommends build-essential wget unzip findutils
          sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*
          
          # 验证md5sum可用性（系统默认内置，确保能正常使用）
          md5sum --version || echo "md5sum已内置，无需安装"
        continue-on-error: false

      # 终极权限+解压+NDK配置（无任何改动，保持之前稳定逻辑）
      - name: Download & Extract Android NDK (r25c, Full Permission)
        run: |
          NDK_VERSION="r25c"
          NDK_MD5="a9a4f843773848921983e148a937f08c"  # 官方MD5，确保压缩包完整
          NDK_URL="https://mirrors.aliyun.com/android-ndk/android-ndk-${NDK_VERSION}-linux.zip"
          NDK_ZIP="/home/runner/ndk.zip"
          NDK_DIR="/home/runner/android-ndk"
          
          # 强制创建目录+全局777权限
          sudo rm -rf ${NDK_DIR}
          sudo mkdir -p ${NDK_DIR}
          sudo chmod -R 777 ${NDK_DIR}
          sudo chown -R runner:runner ${NDK_DIR}
          ls -ld ${NDK_DIR}
          
          # 下载NDK+MD5校验
          echo "📥 下载NDK ${NDK_VERSION}..."
          sudo wget -O ${NDK_ZIP} \
            --timeout=600 --tries=10 --show-progress --progress=bar:force \
            --no-check-certificate
          CALC_MD5=$(md5sum ${NDK_ZIP} | awk '{print $1}')
          if [ "${CALC_MD5}" != "${NDK_MD5}" ]; then
            echo "Error: NDK压缩包损坏！预期MD5: ${NDK_MD5}，实际MD5: ${CALC_MD5}"
            sudo rm -f ${NDK_ZIP}
            exit 1
          fi
          echo "✅ 压缩包完整，MD5匹配"
          
          # 强制解压+全程日志
          echo "📦 解压NDK..."
          sudo unzip -o -u -v ${NDK_ZIP} -d ${NDK_DIR} || {
            echo "Error: 解压失败，查看压缩包内容..."
            sudo unzip -l ${NDK_ZIP}
            sudo rm -f ${NDK_ZIP}
            exit 1
          }
          # 二次赋权
          sudo chmod -R 777 ${NDK_DIR}
          sudo chown -R runner:runner ${NDK_DIR}
          
          # 校验核心工具链目录
          echo "✅ 解压完成，校验核心目录..."
          ls -la ${NDK_DIR}/toolchains/
          if [ ! -d "${NDK_DIR}/toolchains/llvm/prebuilt" ]; then
            echo "Error: 工具链目录缺失！"
            ls -la ${NDK_DIR}/toolchains/llvm/
            exit 1
          fi
          
          # 配置环境变量
          NDK_TOOLCHAIN_BIN=$(find ${NDK_DIR} -name "aarch64-linux-android-clang" -type f | xargs dirname | head -n1)
          export PATH="${NDK_TOOLCHAIN_BIN}:${PATH}"
          echo "NDK_HOME=${NDK_DIR}" >> $GITHUB_ENV
          echo "NDK_TOOLCHAIN_BIN=${NDK_TOOLCHAIN_BIN}" >> $GITHUB_ENV
          echo "${NDK_TOOLCHAIN_BIN}" >> $GITHUB_PATH
          echo "✅ NDK配置完成"
        continue-on-error: false

      # 编译器校验（保持稳定逻辑）
      - name: Verify AArch64 Compiler (Full Check)
        run: |
          echo "=== 确认NDK_HOME ==="
          if [ -z "${NDK_HOME}" ]; then
            echo "Error: NDK_HOME缺失！"
            exit 1
          fi
          echo "NDK_HOME: ${NDK_HOME}"
          
          echo "=== 搜索编译器 ==="
          COMPILER=$(find ${NDK_HOME} -name "aarch64-linux-android-clang" -type f | head -n1)
          if [ -z "${COMPILER}" ]; then
            echo "Error: 编译器缺失！"
            find ${NDK_HOME} -name "*clang*" -type f
            exit 1
          fi
          echo "找到编译器：${COMPILER}"
          
          echo "=== 赋执行权限 ==="
          sudo chmod 777 ${COMPILER}
          if [ ! -x "${COMPILER}" ]; then
            echo "Error: 无执行权限！"
            exit 1
          fi
          
          echo "=== 验证版本 ==="
          ${COMPILER} --version || {
            echo "Error: 编译器执行失败！"
            file ${COMPILER}
            exit 1
          }
          echo "✅ 编译器验证通过"
          echo "COMPILER=${COMPILER}" >> $GITHUB_ENV
        continue-on-error: false

      # 编译内核模块
      - name: Compile AArch64 Kernel Module
        run: |
          echo "=== 确认Makefile ==="
          ls -la | grep -E "Makefile|makefile"
          [ -f Makefile ] || [ -f makefile ] || { echo "Makefile not found!"; exit 1; }

          echo "=== 开始交叉编译 ==="
          make -j$(nproc) V=1 \
            CC="${COMPILER}" \
            CXX=$(find ${NDK_HOME} -name "aarch64-linux-android-clang++" -type f | head -n1) \
            TARGET_API=31
        continue-on-error: false

      # 容错步骤
      - name: Run tests (if supported)
        run: make check
        continue-on-error: true

      - name: Verify release (if supported)
        run: make distcheck
        continue-on-error: true
